# 交接：2026-09-24/25 会话（审计修复批次 + 阿里云接入）

> 给接手的 agent：**先读本文，再读 `docs/aliyun-handoff.md`（阿里云专题）和
> `docs/audit-2026-09-04.md`（历史审计，状态清单已更新）。**
> 所有改动都在**工作区，尚未提交**（44 个文件，+2475/−567）——先 `git status` / `git diff`
> 全量 review，再决定如何分批提交。

## 1. 本次会话做了什么（两部分）

### A. 审计复评后的逐条修复（19 项）

基于 `docs/audit-2026-09-04.md` 的重新核实（P1 真伪、严重性重估），按优先级修复：

| 级别 | 修复 | 关键文件 |
| --- | --- | --- |
| P1 | Ark API Key 探测改零成本：`GET /models` 优先读配额头，仅无头时回退**一次** chat 探测（原来每轮最多 4 次计费请求，且 auto 模式排在免费 arkcli 之前） | `ArkAPIKeyProvider.swift` |
| P2 | Kimi 跨账号 token 配对：按 JWT `sub` 校验 access/refresh，不匹配即丢弃；导入缺 refresh 时清异账号旧值 | `KimiBrowserSession.swift` / `KimiProvider.swift` |
| P2 | LongCat Cookie 来源选择器接通（手动=只用粘贴值；自动=浏览器会话>环境变量） | `LongCatProvider.swift` |
| P2 | 错误正文统一截断：新增 `HTTPErrorSummary`（结构化错误留字段，其余压成 300 字符单行），替换 8 处原样拼 body | `HTTPTransport.swift` 及 6 个 provider |
| P2 | 概览口径统一：概览行改 `menuBarWindow`；`UsageStore.tightestVisibleTab` 单一来源驱动图标/概览/刷新行 | `UsageStore.swift` / `MenuBuilder.swift` / `StatusItemController.swift` |
| P2 | 钥匙串提示中性化：按导入的 Provider 命名，删除虚假的"只保留认证 Cookie"承诺，说明系统弹窗才是真闸门 | 新增 `BrowserKeychainPrompt.swift` |
| P2 | StepFun 会话轮换回写 Keychain；去掉静态 `lastResponse`（rpc 改为返回 response） | `StepFunProvider.swift` / `StepFunBrowserSession.swift` |
| P2 | 导入假成功：StepFun 要 `Oasis-Token`、LongCat 要 `passport_token_key`、Nebula 要 `session`；Nebula userId 必填且绑定同 profile | 三个 `*BrowserSession.swift` |
| P2 | 手动 Cookie 规范化：新增 `CookieHeaderNormalizer`（剥 `Cookie:` 前缀、去属性项、去引号），接入 LongCat/StepFun/SenseNova | 新增 + 3 provider |
| P3 | Cookie 域名边界过滤：SweetCookieKit 默认 `.contains` 会收 `not-stepfun.com`；6 个 importer 全部新增边界过滤 | 新增 `CookieDomainFilter.swift` |
| P3 | `CredentialFileCache` 并发：读改写加锁、临时文件名唯一、返回真实结果；Keychain 结果不再谎报成功 | `CredentialFileCache.swift` / `CookieKeychainStore.swift` |
| P3 | DeepSeek Chrome 扫描加 30 分钟内存缓存；401 作废缓存触发重扫 | `DeepSeekBrowserSession.swift` / `DeepSeekProvider.swift` |
| P3 | `L10n.language` 加锁（消除 `nonisolated(unsafe)` 数据竞争） | `Localization.swift` |
| P3 | 隐藏 provider 不进到期提醒；提醒点击不再跳死胡同；提醒列表不变不重发；补 `$showSummary` 订阅；刷新请求排队而非丢弃（pending 重刷） | `UsageStore.swift` / `ReminderScheduler.swift` / `StatusItemController.swift` |
| P3 | 诊断落盘过滤 cookie/authorization 类 header；StepFun/SenseNova 面板副标题本地化 | `GatewayQuotaHeaders.swift` / `PreferencesWindow.swift` / `Localization.swift` |
| 文档 | `SECURITY.md` 重写（原声称"不持久化 AK/SK"与实现相反）；两个 README 修正商汤状态/打包签名/探测成本/选择器语义；审计文档状态清单更新 | `SECURITY.md` / `README*.md` / `docs/audit-2026-09-04.md` |
| CI | 新增 release 门禁 job：release 构建 → 打包脚本冒烟 → 签名/架构/logo 数量校验 | `.github/workflows/ci.yml` |

### B. 阿里云 Token Plan / Coding Plan 完整接入（专题见 `docs/aliyun-handoff.md`）

阶段 1 骨架（永远报"尚未开通"）→ 完整实现：

- 契约来自官方开源 CLI `github.com/modelstudioai/cli`（**未做抓包猜测**）；
- 三种鉴权：**浏览器登录**（`bl auth login --console` 同款，无需 AK/SK）> AK/SK（ACS3 签名，固定向量测试逐位一致）> sk-sp- 兜底；
- 双套餐自动识别（Token Plan 比例环 / Coding Plan 请求数环），记住后每轮只查一个接口；
- 设置页更名「阿里云百炼」，控制台按钮指向控制台根目录。

## 2. 验证状态（截至本文）

- ✅ `swift build`：0 error 0 warning
- ✅ `swift test`：**219 测试 / 55 suites 全绿**，连续 6 次运行无 flaky
- ✅ 打包部署：`Scripts/package_app.sh` 三次全流程通过，`/Applications/TokenBar.app`
  为 09-25 01:03 构建（arm64、稳定证书签名、`codesign --verify` 通过、11/11 logo）
- ✅ 真实网络验证：sk-sp- Key 打真网关返回规范的 `success:false` 信封 → 说明
  **网关 URL / 表单编码 / 信封解析链路已验证**；其余 10 个 provider 在部署后
  实测全部 `✓`（商汤/DeepSeek/阶跃/智谱/Kimi/OpenCode…）
- ⏳ **未验证：阿里云浏览器登录的完整回调**（需真人登录一次，见 aliyun-handoff §7）
- ⏳ 未验证：菜单/设置窗口的 AppKit 行为、各 Provider 真实浏览器导入——需真机操作

## 3. 仍然开着的审计条目（未修）

F-03（统一重定向策略与响应大小上限）、F-05 残余（失败传播细节）、F-07（无 Task
取消/generation，已用 pending 重刷缓解）、F-11（部分语义）、F-13（响应上限）、
F-14 残余（chat 兜底探测仍在，已降为单次且仅在无配额头时）、F-20（关键风险路径
测试仍薄）、F-21、F-22（Nebula 分页 >2000 条静默截断）。

## 4. 接手注意事项（坑与约定）

1. **不要移除测试的凭据隔离**：`AliyunProviderFetchTests` 是 `.serialized`，且用
   `clearAmbientAliyunCredentials()` 清空/还原本机 Keychain——移除会让测试依赖
   开发者机器状态（曾因此 flaky）。Swift Testing 默认并行，任何碰 `AppSettings.shared`
   的测试都要 `.serialized` 或自包含。
2. **本机状态**：用户 Keychain 里的旧 sk-sp- Key 已在会话中被清掉（设置面板保存空
   字段所致，该 Key 与用量读取无关）；`/Applications` 的 app 已部署最新源码。
3. **打包**：`./Scripts/package_app.sh` 会替换 `/Applications`，默认用本机 Apple
   Development 身份签名；跑之前先 `pkill -f "TokenBar.app/Contents/MacOS/TokenBar"`。
4. **架构约定**：供应商用量优先走"有契约的 API"；只有火山 Ark 那种没有 API 的才
   走供应商 CLI 子进程（sandboxed env）。新增 provider 模仿 LongCat/StepFun 结构：
   `resolveCredential → 判空 → fetch → 静态 parse（可测）`，失败写
   `~/Library/Application Support/TokenBar/<provider>-last-response.txt`（只写名字/状态，不写值）。
5. **文档同步**：改行为要同步 `CHANGELOG.md`（本项目惯例非常详细）、README 两版、
   相关 `docs/*-handoff.md`。

## 5. 建议的下一步

1. 真机验证阿里云浏览器登录（唯一硬缺口）；
2. 把本次工作区改动分批提交（建议按 A/B 两批，各自可独立验证）；
3. 从 §3 的开放条目里挑：优先级最高的是 F-13（响应大小上限）+ F-03（重定向策略）——
   同一处 transport 改造可一并解决；
4. F-20：给 `UsageStore` 的刷新竞态/取消补可注入时间的确定性测试（已被两轮审计点名）。
---

## 6. 收尾状态（2026-09-26 00:5x，第二轮会话）

上一节「建议的下一步」里第 1、2 条已执行完毕：

- **第 1 条（真机验证阿里云浏览器登录）**：已完成，并且发现它根本没通过——`NWListener`
  在挂 VPN 网络扩展的机器上必然 `EINVAL`，所以本文 §2A 的 19 项修复里，阿里云那一整块
  当时是「代码写完、从未跑通」。两轮修复的全部根因与真机响应记录在
  **`docs/aliyun-browser-login-fix.md`**（含 §5 第二轮结论），接口契约与坑更新在
  **`docs/aliyun-handoff.md`**。用户实际登录后确认：月度环 + 到期日 + `Essential` 档位正常显示。
- **第 2 条（分批提交）**：本文 §1 的 A 批（审计修复）与 B/C 批（阿里云）在
  `Localization.swift` / `UsageStore.swift` / 测试 / 两个 README / `CHANGELOG.md` 上
  **文件级交织**（A 批从未提交，B/C 又改了同一批文件的相邻行），无法按文件干净拆开，
  因此本次按「非阿里云文件」+「阿里云与共享文件」两个提交落在同一分支，
  共享文件里同时含两批的改动，PR 描述已注明。
- 第 3、4 条（F-13 响应上限 + F-03 重定向策略、F-20 `UsageStore` 竞态确定性测试）**仍未做**，
  仍按 §3 的清单待接手。

新增的已知产品事实（不是 bug）：**浏览器登录换来的 console 令牌只有几分钟寿命**，
默认 5 分钟刷新一次，所以纯浏览器登录会周期性报 `aliyunConsoleLoginExpired`。
要稳定出数必须配 AK/SK（只需 `modelstudio:GenerateCLIAccessToken`），app 会自动重新换票。

验证基线：`swift build` 干净（`SenseNovaProvider.swift:166` 有一处**先前就存在**的
`conditional cast always succeeds` 警告，与本次无关，未动）；`swift test` 231 / 57 全绿；
`Scripts/package_app.sh` 全流程通过并已装到 `/Applications`。
